const development = {
  connection: {
    host: "localhost",
    user: "root",
    password: "",
    database: "spl_9"
  },
  SERVER_PORT: 8080,
  token: {
    secret: "secret",
    refreshTokenSecret: "refreshTokenSecret",
    tokenLife: 900
  }
};

export default development;
